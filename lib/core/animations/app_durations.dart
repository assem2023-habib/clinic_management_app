class AppDurations {
  AppDurations._();

  static const Duration dFast = Duration(milliseconds: 200);
  static const Duration dNormal = Duration(milliseconds: 350);
  static const Duration dSlow = Duration(milliseconds: 600);
  static const Duration dVerySlow = Duration(milliseconds: 800);

  static const Duration dEntrance = Duration(milliseconds: 550);
  static const Duration dStaggered = Duration(milliseconds: 400);
  static const Duration dShimmer = Duration(milliseconds: 1500);

  static const Duration dPulse = Duration(seconds: 2);
  static const Duration dPulseSlow = Duration(seconds: 4);
  static const Duration dParticle = Duration(seconds: 1);

  static const Duration dRepeatShort = Duration(seconds: 3);
  static const Duration dRepeatLong = Duration(seconds: 6);
  static const Duration dAmbientOrb = Duration(seconds: 20);
  static const Duration dRotate = Duration(seconds: 10);
  static const Duration dBounce = Duration(seconds: 4);

  static const Duration dToast = Duration(milliseconds: 300);
  static const Duration dPress = Duration(milliseconds: 120);
  static const Duration dHover = Duration(milliseconds: 200);
  static const Duration dDotDismiss = Duration(milliseconds: 350);
  static const Duration dSplashPulse = Duration(milliseconds: 1200);
  static const Duration dStarRating = Duration(milliseconds: 600);
  static const Duration dConfirmBtn = Duration(milliseconds: 150);
  static const Duration dSlotList = Duration(milliseconds: 400);
  static const Duration dSupervision = Duration(milliseconds: 400);
  static const Duration dScaleIn = Duration(milliseconds: 500);
  static const Duration dFadeIn = Duration(milliseconds: 500);
}
